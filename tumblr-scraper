#!/usr/bin/env python3
"""Launch the scraper from this file's directory."""

from pathlib import Path
import runpy


if __name__ == "__main__":
    runpy.run_path(str(Path(__file__).resolve().with_name("main.py")), run_name="__main__")
