# Temporary extension

Load `manifest.json` through Firefox `about:debugging` -> `This Firefox` ->
`Load Temporary Add-on...`.

The extension has one toolbar action. It sends a fixed `open` request to the
Native Messaging host and opens or focuses the URL returned by the existing
Tumblr Scraper bridge.
