# Deprecated recovery material

This folder contains retained experiments and false recreations from the UI
recovery work. They are reference-only and are not imported, served, or
packaged by the application.

Production source is outside this folder, in `src/`, `assets/`, `tools/`, and
`tests/`. Do not revive code from here without first comparing it with the
historical source evidence and adding a deterministic test.
